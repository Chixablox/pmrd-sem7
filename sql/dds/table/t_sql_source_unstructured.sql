create table if not exists s_psql_dds.t_sql_source_unstructured (
    id text,
    sale_date text,
    product_name text,
    quantity text,
    price text,
    category text,
    customer_email text,
    region text,
    discount_percent text,
    status text,
    rating text
);